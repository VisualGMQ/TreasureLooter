enum class {{name}} {{{type}}} {
    {{#items}}
    {{{item}}},
    {{/items}}
};
inline constexpr std::uint32_t k{{name}}_Count = {{count}}u;

inline const char* GetEnumName({{name}} value) {
    switch (value) {
    {{#items}}
        case {{name}}::{{{item_name}}}:
            return "{{{item_name}}}";
    {{/items}}
    }
    return "";
}

template <>
inline std::optional<{{name}}> GetEnumFromName<{{name}}>(std::string_view name) {
    {{#items}}
    if (name == "{{{item_name}}}") {
        return {{name}}::{{{item_name}}};
    }
    {{/items}}
    return std::nullopt;
}
