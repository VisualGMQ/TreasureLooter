if (ANDROID)
    set(SDL_SHARED ON)
    set(SDL_STATIC OFF)
else()
    set(SDL_SHARED OFF)
    set(SDL_STATIC ON)
endif()

add_3rdlib(SDL)
add_3rdlib(SDL_ttf)
add_3rdlib(spdlog)
add_3rdlib(stb)
add_3rdlib(imgui)
add_3rdlib(ImNodeFlow)
add_3rdlib(RapidXML)
add_3rdlib(Lyra)
add_3rdlib(Mustache)
add_3rdlib(stduuid)

option(TRACY_ENABLE "" ON)
option(TRACY_ON_DEMAND "" ON)
add_3rdlib(tracy)

set(TMXLITE_STATIC_LIB ON)
set(USE_SDL3_IOSTREAM ON)
add_3rdlib(tmxlite/tmxlite)

if (ANDROID)
    set(LUAU_BUILD_CLI OFF)
else()
    set(LUAU_BUILD_CLI ON)
endif()
set(LUAU_BUILD_TESTS OFF)
set(LUAU_BUILD_WEB OFF)
set(LUAU_WERROR OFF)
set(LUAU_STATIC_CRT OFF)
set(LUAU_EXTERN_C OFF)
set(LUAU_BUILD_SHARED OFF)
add_3rdlib(luau)

add_3rdlib(LuaBridge3)

set(protobuf_INSTALL OFF)
set(protobuf_MSVC_STATIC_RUNTIME OFF)
set(protobuf_LOCAL_DEPENDENCIES_ONLY ON)
add_3rdlib(protobuf)

add_3rdlib(enet)
add_3rdlib(fts_fuzzy_match)

# luau-debugger (DAP server) + its cppdap dependency. Desktop only: the
# debugger embeds a TCP server/threads and is not needed on Android.
if (NOT ANDROID)
    set(CPPDAP_BUILD_TESTS OFF CACHE BOOL "" FORCE)
    set(CPPDAP_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)
    set(CPPDAP_BUILD_FUZZER OFF CACHE BOOL "" FORCE)
    set(CPPDAP_WARNINGS_AS_ERRORS OFF CACHE BOOL "" FORCE)
    set(CPPDAP_INSTALL_VSCODE_EXAMPLES OFF CACHE BOOL "" FORCE)
    set(LUAU_ROOT ${CMAKE_CURRENT_SOURCE_DIR}/luau)
    set(CPP_DAP_ROOT ${CMAKE_CURRENT_SOURCE_DIR}/cppdap)
    add_3rdlib(cppdap)
    add_subdirectory(luau-debugger/debugger)
    set_target_properties(Luau.Debugger PROPERTIES FOLDER 3rdlibs/luau-debugger)
endif()